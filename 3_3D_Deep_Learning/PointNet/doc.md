# pointnet

pointnet is the hello world of 3d deep learning. standard cnns fail on point clouds cos they r unordered. pointnet solves this using max pooling to aggregate features symmetrically!

## the script
i wrote a highly simplified forward pass of pointnet showing the shared mlp (as matrix multiply) n the max pooling step. totally nderstandable n shows the core concept.
