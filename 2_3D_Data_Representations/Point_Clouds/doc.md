# point clouds

point clouds r just unordered sets of (x,y,z) coordinates. they r usually the raw output from lidar or depth cameras.

## the script
just a simple script generating a random point cloud using numpy. it shows that a point cloud is just an Nx3 matrix. very simple n nderstandable.
