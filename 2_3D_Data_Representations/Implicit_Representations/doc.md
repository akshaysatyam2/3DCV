# implicit representations

instead of storing explicit geometry, we use a continuous function that maps 3d coordinates to values like distance or density. nerfs do this!

## the script
i wrote a simple function representing an implicit sphere. u pass in (x,y,z) n it tells u if ur inside or outside.
